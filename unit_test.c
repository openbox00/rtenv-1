#include "unit_test.h"

extern char cmd[HISTORY_COUNT][CMDBUF_SIZE];
extern int cur_his;

extern evar_entry env_var[MAX_ENVCOUNT];
extern int env_count;

void unit_test()
{
	/*
	 * Each unit test must be done here.
	 * Note that Every change of this file must be followed by a change in
	 * qemudbg_init.in file of breaking-point line number.
	 */
}

